# Poetry
```sh
# パッケージをインストールする
poetry add [パッケージ名]

# 開発用パッケージをインストールする
poetry add -D [パッケージ名]

# パッケージをアンインストールする
poetry remove [パッケージ名]

# 既存のpyproject.tomlに従ってパッケージをインストールする
poetry install

# 更新されたのpyproject.tomlに従ってパッケージをインストールする
poetry update
```
